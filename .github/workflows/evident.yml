name: blank

on:
  push:

jobs:
  torch:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        forgive: ["gang", "lifetime", "learned", "basket", "recommendation"]
        rhythm: ["March", "flower", "disease", "reinforce"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar db
